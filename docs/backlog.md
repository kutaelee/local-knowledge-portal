# Backlog

1. Run Ollama model discovery, record its immutable digest and measured dimension, then execute hybrid retrieval evaluation.
2. Add a reconciler state table and deletion/rename/restore tests, plus a watcher supervisor with resume detection.
3. Wire tree-sitter language pack queries for class/function/interface/type chunks.
4. Add log/JSONL record grouping and append-offset checkpoints.
5. Extract Markdown wikilinks, standard links, tags, imports, backlinks, and graph edges.
6. Add document viewer, raw/source view, version diff, Headless Tree, React Flow graph, resizable panes, and full keyboard command palette.
7. Expand Playwright to the ten requested data-dependent scenarios.
8. Back up managed vault/config/manifests alongside database dumps and add backup_run persistence.
9. Add an optional read-only MCP adapter and scheduled task installer.
