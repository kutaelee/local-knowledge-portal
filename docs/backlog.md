# Backlog

1. Complete a 24-hour watcher/worker endurance run and record queue latency and memory trends.
2. Perform a real Windows suspend/resume cycle after `/hooks` trust approval.
3. Add an optional read-only MCP adapter over the existing RAG REST contract.
4. Add an optional Windows Scheduled Task installer with explicit operator approval.
5. Evaluate an optional local reranker only if a larger retrieval corpus demonstrates benefit.
6. Expand tree-sitter grammar coverage as new source languages are registered.
